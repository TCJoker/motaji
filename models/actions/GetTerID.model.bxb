action (GetTerID) {
  description (버스 터미널 ID를 가져온다)
  collect {
    input (src) {
      type (Src)
      min (Optional) max (One)
    }
    input (dst) {
      type (Dst)
      min (Optional) max (One)
    }
  }
  output (TimeResult)
}
